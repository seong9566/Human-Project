<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject.domain.resumes.ResumesDao">

	<insert id="insertLike">
		INSERT INTO resumes(resumes_id,resumes_title,created_at) 
		VALUES(#{resumesId},#{resumesTitle},NOW())
	</insert>

</mapper>