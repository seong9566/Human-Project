<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.miniproject.domain.category.CategoryDao">

	<select id="findById"
		resultType="site.metacoding.miniproject.domain.category.Category">
		SELECT category_id, category_frontend, category_backend,
		category_devops
		FROM category
		WHERE category_id = #{category_id}
	</select>

	<update id="update">
		UPDATE category
		SET category_frontend =
		#{category_frontend}, category_backend =
		#{category_backend},
		category_devops = #{category_devops}
		WHERE category_id = #{category_id}
	</update>

</mapper>