<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject.domain.personal.PersonalDao">

	<select id="findById"
		resultType="site.metacoding.miniproject.domain.personal.Personal">
		SELECT p.personal_name, d.personal_email,
		d.personal_phonenumber, r.resumes_title,
		r.resumes_picture, r.resumes_introduce
		FROM resumes r
		INNER JOIN personal p
		ON r.personal_id = p.personal_id
		INNER JOIN personal_detail d
		ON p.personal_id = d.personal_id
		where resumesId =#{resumesId}
	</select>

</mapper>