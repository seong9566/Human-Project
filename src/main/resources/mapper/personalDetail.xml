<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.miniproject.domain.personal.detail.PersonalDetailDao">

	<select id="findById"
		resultType="site.metacoding.miniproject.domain.personal.detail.PersonalDetail">
		SELECT personal_phonenumber, personal_email
		FROM personal_detail
		where personal_id = #{personal_id}
	</select>

	<update id="update">
		UPDATE personal_detail
		SET personal_email =
		#{personal_email}, personal_phonenumber = #{personal_phonenumber}
		WHERE personal_id = #{personal_id}
	</update>


</mapper>