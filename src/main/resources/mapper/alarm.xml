<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.miniproject.domain.alarm.AlarmDao">

	<select id="findAll"
		resultType="site.metacoding.miniproject.domain.alarm.Alarm">
		SELECT * FROM alarm
	</select>

	<select id="findById"
		resultType="site.metacoding.miniproject.domain.alarm.Alarm">

	</select>
	<select id="findByusersId" resultType="site.metacoding.miniproject.domain.alarm.Alarm">
		SELECT * FROM alarm WHERE users_id = #{usersId}
	</select>


	<insert id="insert" useGeneratedKeys="true">

	</insert>


</mapper>